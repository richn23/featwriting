// Global styles live in app/globals.css.
// This export is kept as an empty string for backward compatibility
// until every page migrates its page-scoped styles out as well.
export const writingStyles = "";
